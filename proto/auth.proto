syntax = "proto3";

package auth;


service AuthService {
    rpc Authenticate(AuthenticateRequest) returns (User);
}

message AuthenticateRequest {
    string tokenz = 1;
}

message User {
    int32 id = 1;
    string email = 2;
}